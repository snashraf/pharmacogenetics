{% if json.id %}

	INSERT INTO LocPGKB VALUES(
		"{{json.id}}",
		"{{json.loc}}",
		"{{json.begin}}",
		"{{json.end}}",
		"{{json.reference}}",
		"{{json.muttype}}"
	);
			
	UPDATE AltAlleles SET AltPGKB = "{{json.alt}}" WHERE VarID = "{{json.id}}";

{% endif %}