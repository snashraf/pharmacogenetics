\documentclass{resume} % Use the custom resume.cls style

\usepackage[left=0.75in,top=0.6in,right=0.75in,bottom=0.6in]{geometry} % Document margins
\usepackage{xcolor}

\name{ {{sampleName}} } % Your name
\address{Generated on \today} % Your address

\begin{document}

{% for json in jsonlist -%}

{% if json.relatedGenes | length > 0 -%}

\begin{rSection}{ {{json.drugName}} }
\item[]

{% for gene in json.relatedGenes -%}
\begin{rSubsection}{ {{gene.geneName}} }{ {{gene.geneDesc}} }{}{}
\item[]
{% if gene.patGuide | length > 0 -%}
\item[] ------------------------------------------------------ Dosing Guideline --------------------------------------------------------\newline
\item[]
\item[] \textbf{ {{gene.patGuide.hapNEW}} } | \textbf{ {{gene.patGuide.hapOLD}} }
\item[] {{gene.patGuide.tex}}
{%- endif %}
{% if gene.patAnnotations | length > 0 -%}
\item[] ---------------------------------------------------- Clinical Annotations -----------------------------------------------------\newline
{% for ann in gene.patAnnotations -%}
\item \textbf{\colorbox{ {%- if "1" in ann.strength or "2" in ann.strength %}{{ann.color}} } {Class {{ann.strength}}}} \textbf{ {{ann.varName}} } \textit{ {{ann.patAllele}} }
\item[] {{ann.annotation | replace("_", "")}} {% endif -%}
{%- endfor %}
\end{rSubsection}

{%- endif %}

{%- endfor %}

\end{rSection}

{%- endif %}

{%- endfor %}

%---------------------------------------------------------------
\large
\textbf{Lower level annotations}\newline
\scriptsize

{% for json in jsonlist -%}

{% if json.relatedGenes | length > 0 -%}

\begin{rSection}{ {{json.drugName}} }
\item[]

{% for gene in json.relatedGenes -%}
{% if gene.patAnnotations | length > 0 -%}
\begin{rSubsection}{ {{gene.geneName}} }{ {{gene.geneDesc}} }{}{}
\item[]
{% for ann in gene.patAnnotations -%}
\item \textbf{\colorbox{ {%- if "3" in ann.strength or "4" in ann.strength %}{{ann.color}} } {Class {{ann.strength}}}} \textbf{ {{ann.varName}} } \textit{ {{ann.patAllele}} }
\item[] {{ann.annotation | replace("_", "")}} {% endif -%}
{%- endfor %}
\end{rSubsection}
{% endif -%}

{%- endfor %}

\end{rSection}

{%- endif %}

{%- endfor %}

\end{document}
