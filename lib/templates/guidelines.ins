{% if json %}

	{% for doc in json %}
		{% for gene in doc.relatedGenes %}
			INSERT INTO Guidelines VALUES(
				"{{doc.id}}",
				"{{gene.id}}",
				"{{did}}",
				"{{doc.source}}"
			);
		{% endfor %}
	{% endfor %}

{% endif %}
