INSERT INTO var_data VALUES
	(
	{{json.location.displayName}},
	{{json.id}},
	{{json.gene.id}},
	{{json.location.name}},
	{{json.location.begin}},
	{{json.location.end}},
	{{json.location.reference}},
	{{json.location.variants|join(",")}},
	{{json.type}},
	{{json.altNames.synonym|join(",")}}
	);