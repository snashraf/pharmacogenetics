INSERT INTO Haplotypes VALUES(
	"{{json.gene.id}}",	
	"{{json.id}}",
	"{{json.symbol}}",
	"{{json.name}}",
	"{{json.hgvs}}"
);

{% for doc in json.alleles %}

	INSERT INTO HapVars (
		"{{doc.linkedObjects.variant.id}}",
		"{{doc.location.displayName}}",
		"{{doc.allele}}",
		"{{doc.type}}"
	);

{% endfor %}
